#!/usr/bin/env bash
# Helper script for {{SKILL_NAME}}
# Provides environment detection and auxiliary functions.
# All output is JSON to stdout, errors to stderr.

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

case "${1:-}" in
  preflight)
    # TODO: Add actual environment checks.
    # Check for required tools, services, login status, etc.
    # Return {"ready": false, ...} with hints if something is missing.
    echo '{"ready": true, "dependencies": {}, "hint": "All checks passed"}'
    ;;
  *)
    echo '{"error": "unknown_command", "hint": "Usage: helper.sh preflight"}' >&2
    exit 2
    ;;
esac
